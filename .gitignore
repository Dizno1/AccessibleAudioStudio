# Generated by scripts/prepare-dist.js before every desktop dev/build run.
# Never commit this — it's a disposable copy of index.html + app/, and
# committing it is exactly how frontendDist drift/staleness bugs happen.
/dist/

# Node dependencies for the Tauri CLI (see package.json).
/node_modules/

# See src-tauri/.gitignore for Rust build output.
